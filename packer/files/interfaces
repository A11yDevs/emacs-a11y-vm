# /etc/network/interfaces — Configuração de rede para Debian A11y VM
#
# Interface de loopback
auto lo
iface lo inet loopback

# Interface de rede primária (DHCP)
# eth0 é o nome estável configurado via net.ifnames=0 biosdevname=0 no GRUB
allow-hotplug eth0
iface eth0 inet dhcp
